mod strings;
mod characters;

fn main() {
    characters::literals();
    characters::conversion();
    strings::raw();
    strings::ownership_raw();
    strings::length_and_capcity();
    strings::substring();
    strings::string_type();
    strings::string_ownership();
}
